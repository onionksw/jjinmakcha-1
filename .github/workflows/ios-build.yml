name: iOS build check

# 서명 없이 시뮬레이터용으로 빌드만 성공하는지 확인하는 용도.
# 공개 저장소라 macOS 러너가 무료로 제공됨.
on:
  push:
    branches: [main]
    paths:
      - 'ios/**'
      - 'services/**'
      - 'components/**'
      - 'App.tsx'
      - 'capacitor.config.ts'
      - 'package.json'
      - '.github/workflows/ios-build.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm ci

      - name: Build web assets (native mode)
        run: npm run build:native 2>&1 | tee build.log

      - name: Sync iOS
        run: npx cap sync ios 2>&1 | tee -a build.log

      - name: Upload log (always, for debugging)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log

      - name: Build iOS app (unsigned, simulator SDK)
        run: |
          xcodebuild \
            -project ios/App/App.xcodeproj \
            -scheme App \
            -sdk iphonesimulator \
            -configuration Debug \
            CODE_SIGNING_ALLOWED=NO \
            build
